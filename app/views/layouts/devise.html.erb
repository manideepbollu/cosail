<!DOCTYPE html>
<html>

<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Join CoSail</title>
    <%= stylesheet_link_tag    'login', media: 'all', 'data-turbolinks-track' => true %>
    <%= stylesheet_link_tag    'login', media: 'all', 'data-turbolinks-track' => true %>

    <!-- Fontawesome CDN -->
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/font-awesome/4.5.0/css/font-awesome.min.css">

    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
</head>

<body class="cosailblue-bg">

<%= content_tag(:div, flash[:error], :id => "flash_error", :class => 'login-flash') if flash[:error] %>
<%= content_tag(:div, flash[:notice], :id => "flash_notice", :class => 'login-flash') if flash[:notice] %>
<%= content_tag(:div, flash[:alert], :id => "flash_alert", :class => 'login-flash') if flash[:alert] %>

<div class="middle-box text-center loginscreen animated fadeInDown <%= 'middleboxlogin' if controller_name == 'sessions' %> ">
    <div>
        <div>

            <h1 class="logo-name"><%= image_tag 'cosail-internal-logo.png', alt: 'CoSail', width: 300, title: 'CoSail', class: 'cosail-home-logo' %></h1>
        </div>
        <%= yield %>
    </div>
</div>

</body>

<% content_for :javascript do %>
<script type="text/javascript">
    $(document).ready(function(){

        $('#data_1 .input-group.date').datepicker({
            todayBtn: "linked",
            keyboardNavigation: false,
            forceParse: false,
            calendarWeeks: true,
            autoclose: true,
            dateFormat: 'dd/mm/yyyy'
        });

    });
</script>

<% end %>

<%= yield :javascript %>

</html>
